// import { useSelector } from "react-redux";
import "./SplashPage.css";

function SplashPage() {
    // const sessionUser = useSelector((state) => state.session.user);

    return (
        <div className="splash-div">
            <div className="horse-head" alt="">
            </div>
        </div>
    )
};

export default SplashPage;